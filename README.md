# version-controller

NexCore 产品线统一版本控制器。

**只存编译产物,不存源码。** 每个产品通过 tag 前缀区分:

```
linkflow-v1.0.0
nexcorepay-v2.0.0
nexcoreproxy-v3.0.0
```

下载产物(公开,无需认证):
```
https://github.com/DoBestone/version-controller/releases/download/{tag}/{filename}
```
